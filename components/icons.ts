import { Zap, Ghost, Trophy, Popcorn, Leaf, type LucideIcon } from "lucide-react";
import type { IconName } from "@/lib/data";

export const iconMap: Record<IconName, LucideIcon> = {
  zap: Zap,
  ghost: Ghost,
  trophy: Trophy,
  popcorn: Popcorn,
  leaf: Leaf,
};
